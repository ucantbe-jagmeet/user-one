import { FC } from "react";
import Navbar from "../component/Navbar";

const Home: FC = () => {
  return (
    <div className="h-[100vh] bg-[--primary-light-blue] ">
      <Navbar />
    </div>
  );
};

export default Home;
